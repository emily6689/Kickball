<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Paul and Em's Amazing Team</title>
    <link rel="stylesheet" href="home.css" />
  </head>

  <body>
    <h1><%= @players[0]["position"] %></h1>
    <table>

     <% @players.each do |player| %>

      <tr>
          <td><%= player["first_name"] %> <%= player["last-name"] %></td>
          <td> <%= player["team_name"] %></td>
      </tr>

     <%end%>

   </table>

   <p><a href='/'>All Teams</a>

  </body>
</html>
